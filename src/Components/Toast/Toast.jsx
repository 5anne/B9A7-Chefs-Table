

const Toast = (toast) => {
    console.log(toast);
    return (
        <div className="toast">
            <div className="alert alert-info">
                <span>ALready exist!</span>
            </div>
        </div>
    );
};

export default Toast;